import { login } from "@/app/actions/auth";

export default async function LoginPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) { const error = (await searchParams).error; const message = error === "config" ? "Missing login configuration in .env.local." : error === "invalid" ? "Email or password is incorrect." : null; return <main className="login"><section><p className="eyebrow">NIRA WHATSAPP OPERATIONS</p><h1>Control centre</h1><p>Sign in to manage citizen-support operations.</p>{message && <p className="login-error">{message}</p>}<form action={login}><label>Email<input name="email" type="email" placeholder="tusiimesam@gmail.com" required/></label><label>Password<input name="password" type="password" required/></label><button className="button" type="submit">Sign in</button></form><small>Temporary testing access. Replace with Supabase Auth before production.</small></section></main>; }
