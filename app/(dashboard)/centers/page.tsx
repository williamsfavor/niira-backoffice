import { MapPin, Plus } from "lucide-react";
import { Status } from "@/components/ui";
import { getDashboardData } from "@/lib/supabase";
import { createCenter, toggleCenter } from "@/app/actions/operations";
export default async function CentersPage() { const { centers } = await getDashboardData(); return <section className="page"><div className="page-title"><div><p className="eyebrow">WHATSAPP LOCATION SEARCH</p><h2>Collection centres</h2><p className="page-copy">Keep centre details accurate so location sharing returns the right collection point.</p></div><span className="button"><Plus size={16}/> Add centre below</span></div><details className="panel create-panel"><summary>Add a collection centre</summary><form action={createCenter} className="form-grid"><label>Name<input name="name" required/></label><label>District<input name="district" required/></label><label className="full">Address<input name="address" required/></label><label>Phone<input name="phone" type="tel"/></label><label>Hours<input name="hours" placeholder="Mon–Fri, 8:00 AM–5:00 PM"/></label><label className="check"><input name="is_active" type="checkbox" defaultChecked/> Available in WhatsApp location search</label><button className="button" type="submit">Save centre</button></form></details><article className="panel"><div className="table-wrap"><table><thead><tr><th>Centre</th><th>District</th><th>Address</th><th>Hours</th><th>Availability</th><th/></tr></thead><tbody>{centers.map((centre) => <tr key={centre.id}><td><strong>{centre.name}</strong></td><td>{centre.district}</td><td><span className="inline"><MapPin size={15}/>{centre.address}</span></td><td>{centre.hours ?? "—"}</td><td><Status value={centre.is_active ? "Active" : "Inactive"}/></td><td><form action={toggleCenter}><input type="hidden" name="id" value={centre.id}/><input type="hidden" name="is_active" value={String(!centre.is_active)}/><button className="secondary">{centre.is_active ? "Deactivate" : "Activate"}</button></form></td></tr>)}</tbody></table></div></article></section>; }
