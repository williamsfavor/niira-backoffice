import { Clock3 } from "lucide-react";
import { Status } from "@/components/ui";
import { getDashboardData } from "@/lib/supabase";
import { claimTicket, resolveTicket } from "@/app/actions/operations";
export default async function TicketsPage() { const { recentTickets } = await getDashboardData(); return <section className="page"><p className="eyebrow">CITIZEN ESCALATIONS</p><h2>Support tickets</h2><p className="page-copy">Claim, track, and resolve cases created when WhatsApp self-service cannot help a citizen.</p><div className="filters"><button className="selected">All tickets</button><button>Open</button><button>In progress</button><button>Resolved</button></div><div className="queue">{recentTickets.map((ticket) => <article className="queue-item" key={ticket.ticket_id}><div className="ticket-icon"><Clock3 size={18}/></div><div className="queue-body"><div><span className="mono">{ticket.ticket_id}</span><Status value={ticket.status}/></div><h3>{ticket.issue_text}</h3><p>{ticket.phone_number} · Created {new Intl.DateTimeFormat("en-UG", { dateStyle: "medium" }).format(new Date(ticket.created_at))}</p>{ticket.status !== "Resolved" && <details className="resolve"><summary>Resolve ticket</summary><form action={resolveTicket}><input type="hidden" name="ticket_id" value={ticket.ticket_id}/><textarea name="resolution_note" placeholder="What was done to resolve this?" required/><button className="button" type="submit">Mark resolved</button></form></details>}</div>{ticket.status === "Open" && <form action={claimTicket}><input type="hidden" name="ticket_id" value={ticket.ticket_id}/><button className="secondary">Claim ticket</button></form>}</article>)}</div></section>; }
